#include <iostream>
using namespace std;

int main()
{
    int s = 60, m = 60, h = 24, d = 365;
    long long y = 10, x;
    x = 1 * d * h * m * s;
    x = 10 * d * h * m * s;
    x = 100 * d * h * m * s;
    x = 1000 * d * h * m * s;
    x = 10000 * d * h * m * s;
    x = 100000 * d * h * m * s;
}