#include <iostream>
using namespace std;
int main()
{
    int n;

    n = sizeof(int);
    n = sizeof(short);
    n = sizeof(long);
    n = sizeof(char);
    n = sizeof(float);
    n = sizeof(double);
}